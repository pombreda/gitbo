
<div class='jumbotron'>
 <p id="notice text-success"><%= notice %></p> 
   <h1>Gitbo</h1>
   <p class='lead'>Get paid to fix the most asked for Github issues</p> 
</div>

<div class='row'>
  <div class='span6'>
    <h3>Trending repos</h3>
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Popularity</th>
          <th>Open Issues</th>
          <th></th>
        </tr>
      <% @repos.sort_by(&:popularity).reverse[1..5].each do |repo| %>
        <tr>
          <td><%= link_to repo.owner_name, owner_repos_path(repo.owner_name) %> / <%= link_to repo.name, owner_repo_path(repo.owner_name, repo.name) %></td>
          <td><%= repo.popularity %></td>
          <td><%= repo.open_issues %></td>
        </tr>
      <% end %>  

      </thead>
    </table>
  </div>

  <div class='span6'>
    <h3>Trending issues</h3>
    <table class="table table-hover">
        <thead>
          <tr>
            <th>Issue Name</th>
            <th>Repo Name</th>
            <th>Popularity</th>
          </tr>
            <% @issues.sort_by(&:popularity).reverse[1..5].each do |issue| %>
            <tr>
              <td><%= link_to issue.title, owner_repo_gitnumber_path(issue.repo.owner_name, issue.repo.name, issue.git_number) %></td>
              <td><%= link_to issue.repo.name, issue.repo %></td>
              <td><%= issue.comment_count %></td>
            </tr>
            <% end %>
        </thead>
    </table>
    <br />
  </div>
</div>

