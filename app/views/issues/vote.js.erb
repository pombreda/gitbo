// find the table row that was clicked on
// in that row, select the vote column
// replace it's value with the value of @issue.net_votes
// if (window.location.pathname == "/issues"){
// if ($("tr#issue_<%= @issue.id %> td.votes").length > 0){
  $("tr#issue_<%= @issue.id %> td.votes").html("<%= @issue.net_votes %>");  
// } else {
  $(".discussion-stats span.net_votes").html("<strong><%= @issue.net_votes %></strong> Scores");   
    //change the color of buttons upon click 
    // $("#upvote_issue").removeClass().addClass('btn btn-warning');
// })
