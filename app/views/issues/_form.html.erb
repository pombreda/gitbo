<%#= form_for @issue, remote: true do |f| %>
  <%#= f.text_fied :title %>
  <%#= f.submit %>
<%# end %>

<%= form_for @issue do |f| %>
  <% if @issue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@issue.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
        <% @issue.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <p>
      <%= f.fields_for :repos, @issue.repo do |f| %>
        <%= f.label :owner_name %><br />
        <%= f.text_field :owner_name %><br />
        <%= f.label :name %><br />
        <!-- text_area leaves a block. specify rows for -->
        <%= f.text_area :name, :rows => 3, :class => "field span4" %><br />
    </p>
    <p><%= f.label :git_number %><br />
    <%= f.text_field :git_number %><br />
    </p>
    <%= f.submit 'Create', :class => 'btn btn-success'  %>
    <% end %>
<% end %>

